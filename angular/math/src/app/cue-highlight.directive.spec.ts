import { CueHighlightDirective } from './cue-highlight.directive';

describe('CueHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new CueHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
